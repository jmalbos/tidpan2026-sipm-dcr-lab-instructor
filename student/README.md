# Student Code

Edit `analysis.py` during the lab.

The notebook calls the functions in this file, and the automated tests will
check the same functions. Keep the function names and arguments unchanged.

Recommended order:

1. `load_data`
2. `timestamps_to_seconds`
3. `compute_live_time`
4. `compute_interarrival_times`
5. `compute_dark_count_rate`
6. `convert_rate_to_mhz_per_mm2`
7. `tag_burst_events`
8. `estimate_baseline`
9. `compute_negative_amplitudes`
10. `tag_crosstalk_candidates`
11. `tag_afterpulse_candidates`

Run the notebook after each few functions rather than waiting until the end.
